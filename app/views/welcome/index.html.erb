<%= render :partial => 'shared/tips' if @users.empty? || @projects.empty? || @entries.empty? %>

<%= render :partial => 'users/list' if !@users.empty? %>

<%= render :partial => 'projects/list' if !@projects.empty? %>

<%= render :partial => 'entries/list' if !@entries.empty? %>